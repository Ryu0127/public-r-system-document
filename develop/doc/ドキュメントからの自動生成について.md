
### .ps1ファイルの実行
```powershell
# PowerShellを開いて実行
.\watch-requirements.ps1
```

> [!note] 注意
> Windowsはデフォルトでps1ファイルの実行をブロックしています。
> **最初に一度だけ**以下を実行してください。
> ```powershell
> # PowerShellを管理者として起動して実行
> Set-ExecutionPolicy RemoteSigned -Scope CurrentUser
> ```
